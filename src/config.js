/**
 * qixi-snow — 全局配置
 *
 * 这里是唯一需要修改的地方：文案、配色、粒子数量、时间轴节奏。
 * 不需要理解 Three.js 代码也可以安全调整。
 */

export const CONFIG = {
  /* ------------------------------------------------------------------ *
   * 1. 文案（改这里就够了）
   * ------------------------------------------------------------------ */
  text: {
    title: '雪 and 潇',                          // Scene 5（署名）
    subtitle: '我们的第一个七夕',                  // Scene 6
    finale: '愿每一次抬头，都有星光，更有你',        // Scene 7
    entry: '点击开启我们的第一个七夕',              // 启动入口
    entryHint: 'Click to begin · 建议全屏观看',
    replay: 'Replay',
  },

  /* ------------------------------------------------------------------ *
   * 2. 配色（冰蓝 / 蓝紫 / 银白，深蓝黑夜）
   * ------------------------------------------------------------------ */
  colors: {
    bgTop: '#03040c',        // 背景上方
    bgBottom: '#070d1c',     // 背景下方
    nebula: '#12224a',       // 极淡星云
    heartCore: '#eaf4ff',    // 心的高光（银白）
    heartMid: '#7ec8ff',     // 心的主色（冰蓝）
    heartDeep: '#5a63ff',    // 心的内部深处（蓝紫）
    heartEdge: '#8f7bff',    // 心的边缘（蓝紫）
    glow: '#6fa8ff',         // 内部体积光
    star: '#cfe4ff',         // 星光
    starWarm: '#ffe9d0',     // 少量暖色星（点缀）
    dust: '#bcd9ff',         // 雪尘
    butterflyA: '#7fd4ff',   // 蝶翼靠身体一侧
    butterflyB: '#8f8cff',   // 蝶翼翼尖
    butterflyEdge: '#e8f6ff',// 翼缘高光
  },

  /* ------------------------------------------------------------------ *
   * 3. 数量与规模（性能相关）
   * ------------------------------------------------------------------ */
  counts: {
    heart: 150000,      // 主体心形粒子
    innerGlow: 1100,    // 心内部柔光大粒子
    dust: 2500,         // 空间雪尘
    starsNear: 1150,    // 近层星
    starsFar: 2000,     // 远层星
    butterflies: 7,     // 蝴蝶数量（宁少而精）
  },

  heart: {
    width: 3.35,        // 心的横向宽度（世界单位）
    depth: 0.62,        // 心的半厚度（立体感）
    center: [0, 0.62, 0],
    shellRatio: 0.72,   // 贴近表面的粒子比例（其余在体积内部）
    escapeRatio: 0.13,  // 会离开表面再回归的粒子比例
    disperseRatio: 0.34,// 终章向外扩散的粒子比例（其余保留主体）
    breathe: 0.016,     // 呼吸幅度
  },

  butterfly: {
    minScale: 0.21,      // 半翼展（世界单位）
    maxScale: 0.37,
    minRadius: 1.85,
    maxRadius: 3.15,
    flapSpeed: [5.0, 7.2],
  },

  camera: {
    fov: 42,
    /**
     * 时间轴关键帧：[时间, 相机位置, 注视点]
     * 注视点略低于心的中心 → 心位于画面上部，下方留给字幕。
     */
    keys: [
      [0.0,  [0.00, 0.55, 15.6], [0.00, -0.10, 0]],
      [8.0,  [-0.62, 0.38, 11.0], [0.00, -0.12, 0]],
      [13.5, [0.30, 0.20, 8.30], [0.00, -0.14, 0]],
      [20.0, [0.55, 0.10, 7.75], [0.02, -0.14, 0]],
      [26.0, [-0.42, 0.18, 7.95], [0.00, -0.14, 0]],
      [28.6, [-0.10, 0.28, 8.70], [0.00, -0.11, 0]],
      [34.0, [0.14, 0.22, 8.35], [0.00, -0.13, 0]],
    ],
    driftAmp: 0.055,     // 极轻微的“手持”漂移
  },

  /* ------------------------------------------------------------------ *
   * 4. 时间轴（秒）— 整体 36s，最后 4s 稳定画面适合录屏收尾
   * ------------------------------------------------------------------ */
  timeline: {
    duration: 36.0,

    stars:      { in: [0.3, 2.8] },
    dust:       { in: [1.0, 4.2] },

    heartFade:  { in: [3.4, 6.2] },     // 粒子可见度
    form:       { in: [3.4, 12.8] },    // 聚合：四周 → 中心
    alive:      { in: [10.5, 14.5] },   // 表面流动 / 离散回归 / 呼吸
    innerGlow:  { in: [11.0, 15.2] },

    butterflies:{ in: [13.0, 18.5] },   // 7 只依次进入

    text1:      { in: 15.6, out: 20.6 },
    text2:      { in: 21.6, out: 26.4 },
    text3:      { in: 27.4 },

    pulse:      { at: 27.7, attack: 0.42, decay: 2.1 },
    disperse:   { in: [28.0, 32.0] },
    hold:       { from: 32.0 },   // 之后画面保持稳定（仅剩呼吸与蝴蝶）
  },

  /* ------------------------------------------------------------------ *
   * 5. 画质 / 后处理
   * ------------------------------------------------------------------ */
  quality: {
    maxDPR: 1.5,
    msaa: 0,          // 粒子与柔光本身没有硬边，不需要 MSAA
    bloomScale: 0.6,  // bloom 在半分辨率上计算（更省，也更柔）
    exposure: 1.02,
    bloom: { strength: 0.70, radius: 0.86, threshold: 0.58 },
    grade: {
      vignette: 0.62,
      grain: 0.035,
      chromatic: 0.0016,
      lift: 0.006,
      contrast: 1.045,
      saturation: 1.03,
    },
    adaptive: true,     // 帧率过低时自动降 DPR / bloom 分辨率
  },

  /* 本作品是纯视觉、完全静音的版本：网页不播放任何声音，
     背景音乐在后期剪辑中单独加入。 */

  /** 开发用：跳到时间轴某一秒开始（0 = 正常） */
  debugStartTime: 0,
};

export default CONFIG;
